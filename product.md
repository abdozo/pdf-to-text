# ورّاق: قرارات المنتج الحالية

آخر تحديث: 20 سبتمبر 2026.

ورّاق تطبيق سطح مكتب شخصي لتحويل الكتب العربية المصوّرة بصيغة PDF إلى نص قابل للتحرير، ومراجعته مقابل الأصل، ثم تصديره إلى Word أو Markdown أو HTML تفاعلي. التطبيق مبني بـPython وPySide6 وQt Quick، ولا توجد له واجهة ويب مستقلة.

## مبادئ المنتج

- أمانة النص أهم من الإكمال أو التصحيح التلقائي.
- في النسخ والترجمة تقابل كل صفحة مصدر صفحة في الناتج. التلخيص له نتائج مستقلة لكل دفعة، وعدد أوراقه تابع للمادة.
- النسخ يحافظ على الصفوف المطبوعة؛ الترجمة تحفظ حدود الصفحات دون فرض مقابلة سطر بسطر؛ التلخيص يجمع مادة الدفعة في أربعة أرباع موثقة بصفحات PDF.
- نجاح التحويل لا يساوي اعتماد المراجعة البشرية.
- تحفظ كل نتيجة فور وصولها، وتبقى المهام قابلة للتوقف والاستئناف.
- المكتبة والبيانات محلية، ولا تُرسل إلى Gemini إلا الصفحات أو الملخصات التي يختار المستخدم معالجتها.

## رحلة الاستخدام

1. يضيف المستخدم مسار كتاب PDF إلى المكتبة المحلية دون أن ينسخ التطبيق الملف.
2. يحدد العملية، ونطاق الصفحات، وعدد الصفحات في الاستعلام الواحد، ومفتاح Gemini المسمّى، والنموذج، وبرومبت العملية. يحدد لغة الناتج واتجاه كتابتها للترجمة أو تفصيل الصياغة للتلخيص، مع حفظ المعلومات المهمة في جميع الدرجات.
3. يرسل التطبيق صفحات كل دفعة كصور مستقلة داخل استعلام واحد، ثم يتحقق من أرقامها ويحفظ نتيجة كل صفحة في النسخ والترجمة، أو ملخص الدفعة الموحد في التلخيص.
4. يراجع المستخدم الصفحة الأصلية والنص في وضع جنبًا إلى جنب.
5. يصحح النص في محرر WYSIWYG واحد للصفحة ثم يعتمدها.
6. يصدر صفحة أو نطاقًا أو كتابًا إلى Word أو Markdown أو HTML، مع فحص عدد صفحات Word عند توفر LibreOffice. يعمل ملف HTML وحده ويعرض صفحة واحدة في كل مرة، مع أزرار تنقّل وحقل انتقال إلى رقم الصفحة وبحث يربط كل نتيجة بصفحتها.

## قرار واجهة المراجعة

- «جنبًا إلى جنب» هو الوضع الوحيد، لذلك لا يظهر مفتاح لاختيار طريقة المطابقة.
- محرر النص في الجانب الأيسر، وصورة الصفحة الأصلية في الجانب الأيمن.
- لكل صفحة محرر واحد يضم جميع الأسطر، ويحافظ كل سطر على فاصل مستقل.
- المحرر هو Tiptap المضمّن محليًا داخل تطبيق الديسكتوب، مع شريط أدوات صغير خاص بورّاق.
- يدعم العناوين والخطوط والأحجام والعريض والمائل والتسطير والشطب والاقتباس والقوائم والنص المرتفع والمنخفض والألوان والمحاذاة وإزالة التنسيق.
- ألغيت أدوات المطابقة فوق الأصل والشفافية والإزاحة والطبقات.
- ألغيت إعدادات بنية الصفحة والمناطق والأعمدة من الواجهة.
- يعرض زر الصفحة «تحويل الصفحة» ما لم تكن الصفحة قد تحولت بنجاح، ثم يعرض «إعادة تحويل الصفحة».
- كل الأزرار والعناصر القابلة للنقر تستخدم مؤشر اليد عند مرور الماوس.

## التخزين والتصدير

يُحفظ HTML المنظف الصادر عن المحرر في سجل الصفحة داخل SQLite. تحتفظ قاعدة البيانات بتمثيل نصي للأسطر للتوافق، بينما تستخدم صيغ التصدير محتوى المحرر المنسق. يحافظ Word وHTML على التنسيق المرئي الأساسي، ويجمع Markdown نص الصفحات دون إضافة بيانات داخلية إلى الملف. أي تعديل على صفحة معتمدة يعيدها إلى حالة تحتاج مراجعة.

يبقى ملف PDF في مكانه الأصلي، وتحفظ قاعدة البيانات مساره المطلق فقط. لا ينشئ التطبيق نسخة منه داخل بيانات المستخدم أو حزمة البرنامج. تكشف شاشة المكتبة المسار المفقود، وتتيح شاشة الكتاب إعادة ربط PDF من نافذة اختيار الملفات الأصلية للنظام مع بقاء التحويلات والمراجعات. يرفض التطبيق الملف البديل إذا اختلف عدد صفحاته حتى لا ترتبط النصوص بصفحات خاطئة. تتيح الشاشة نفسها تعديل اسم الكتاب أو حذفه من المكتبة، ولا يحذف الحذف ملف PDF من الجهاز. تحفظ النسخ الاحتياطية قاعدة البيانات والنصوص والإعدادات ولا تضم ملفات PDF.

يعيد Gemini مخطط صفحة يحتوي على `is_blank` و`printed_page` ومصفوفة `lines`. عند إرسال عدة صفحات، يعيد مصفوفة `pages` ويضع رقم PDF الصريح في كل نتيجة. يمثل كل عنصر في `lines` صفًا مطبوعًا واحدًا في النسخ، وكتلة نصية مرنة في الترجمة. يحول التطبيق Markdown الداخلي إلى نص منسق في محرر المراجعة. لا يطلب المسار من Gemini إحداثيات أو مناطق.

## البنية التقنية

| الجزء | التقنية |
|---|---|
| الواجهة | PySide6 وQt Quick/QML |
| محرر النص | Tiptap داخل Qt WebEngine، بحزمة محلية |
| الربط مع المحرر | Qt WebChannel |
| قاعدة البيانات | SQLite بترحيلات مرقمة |
| PDF والصور | PDFium وPillow |
| التحويل | Google Gen AI SDK |
| الأسرار | مخزن كلمات المرور في نظام التشغيل |
| Word | python-docx وLibreOffice للفحص |
| Markdown وHTML | ملفات UTF-8 محلية مستقلة |
| حزمة macOS | pyside6-deploy وNuitka |
| مثبت Windows | pyside6-deploy وNuitka وInno Setup |

لا يستخدم التطبيق FastAPI أو Uvicorn أو متصفحًا خارجيًا أو خادم HTTP محليًا.

يعرض التطبيق مفاتيح Gemini كقائمة مسمّاة فقط، ولا يطلب من المستخدم إنشاء مشروع Google داخل ورّاق. ترتبط بيانات الاستخدام وحدود الطلبات بالمفتاح مباشرة. حذف المفتاح يزيل بياناته المحلية وسرّه من مخزن كلمات المرور في النظام، لكنه لا يلغي المفتاح من Google AI Studio.

## ما ليس ضمن النطاق الحالي

- الحسابات أو التعاون بين عدة مستخدمين.
- التخزين السحابي للمكتبة أو مزامنتها.
- التبديل التلقائي بين مفاتيح API.
- محركات OCR محلية أو تنزيل نماذج محلية.
- استكمال النصوص الدينية من مصادر خارج صورة الصفحة.
- إعادة بناء التخطيط المكاني للأعمدة والحواشي داخل شاشة المراجعة.


## التلخيص بالأرباع ومراجعة الملخصات

يعيد مسار التلخيص `source_pages` ومصفوفة `sheets`. تضم كل ورقة أربعة أقسام مستقلة: `understanding` و`insights` و`critique` و`applications`؛ ولكل فكرة عنوان وصفي ونص وإحالات `pdf_pages` إلزامية. تستند التعليمات إلى البرومبت الأصلي للمستخدم، دون المقارنة المعرفية والوسوم، ودون ادعاء الرجوع إلى مراجع خارجية. الأرقام هي صفحات PDF ابتداءً من 1 فقط.

يحفظ جدول `summaries` النتائج بعيدًا عن صفحات النسخ والترجمة. يُحفظ الملخص وتقدم الدفعة في معاملة واحدة، وتبقى الدفعات قابلة للتعليق والاستئناف وإعادة محاولة الفاشل. لا تُرسل الدفعات السابقة كسياق.

مراجعة الملخصات عملية يدوية لربط نتائج مختارة من كتاب واحد وإزالة التكرار، مع إبقائها وحدات مستقلة. يقسم التطبيق الاختيار إلى وحدات محفوظة في `summary_review_units`، ويقسم الملخص الطويل دون إسقاط مادته. يختار المستخدم عدد الملخصات في الاستعلام (الافتراضي 3)، ويعالجها معًا مع سياق محدود من آخر ورقة سابقة مراجعة وأول ورقة تالية. عدد أوراق الناتج مرن، مع الحفاظ على ترتيب الموضوعات وإحالات PDF. حد الحجم يخص الاستعلام، ولا يرفض الاختيار الكبير كله. تحفظ كل نتيجة وتقدمها في معاملة واحدة؛ وعند فشل دفعة تتوقف السلسلة حتى استئنافها. تبقى الأصول دون تعديل، وتُعرض نتائج كل مراجعة في سلسلة منفصلة. تدعم أوراق المراجعة أرباعًا فارغة إذا لم يبق فيها جديد بعد حذف التكرار. لا تتضمن المرحلة شجرة فهرس أو اكتشاف حدود الفصول أو بحثًا مرجعيًا خارجيًا.

التلخيص والمراجعة يكتبان الفكرة والاقتباس والاستنتاج مباشرة، دون مقدمات «قال المؤلف» ومثيلاتها. تستخدم عناوين قصيرة لتمييز الاقتباس والاستنتاج والتطبيق المقترح، مع حفظ النص الحرفي للاقتباسات.

تعرض شاشة الملخصات شبكة أرباع مع روابط لصفحات المصدر، وتصدر جميع الملخصات المحددة بأوراقها إلى HTML أو Word بالأرباع أو Markdown بأقسام متتابعة، أو المعروض عند عدم وجود تحديد. الأرباع قابلة للامتداد مع طول المحتوى؛ ليست كل ورقة منطقية مضمونة في صفحة A4 واحدة. نتائج الشاشة للعرض والتصدير حاليًا.

التلخيص الأولي والمراجعة يكتبان الأفكار مباشرة دون سرد أفعال المؤلف. يفحص التطبيق بقاء عبارتي المؤلف والمصنف خارج الاقتباسات؛ وعند وجودهما يطلب تصحيحًا لغويًا محدودًا (محاولتان كحد أقصى) قبل الحفظ. كل طلب تصحيح مسجل في الاستهلاك. الإحالات وبنية الأقسام ثابتة أثناء التصحيح، وتُرفض التغييرات في الاقتباسات المعلّمة والأرقام. تحظر التعليمات تغيير المعنى والقيود والأسماء ودرجة الجزم؛ وتبقى مراجعة المعنى بشريًا ضرورية عند التدقيق العلمي. النتائج القديمة لا تتغير تلقائيًا.

بقاء عبارة عن المؤلف بعد محاولتي التصحيح لا يوقف المعالجة؛ تُحفظ النتيجة مع ملاحظة صياغية وتستمر الدفعات التالية. من شاشة الملخصات يمكن حذف سلسلة مراجعة سابقة بعد تأكيد الحذف، مع حفظ الأصول والمراجعات الأخرى وسجل الاستهلاك. لا يمكن حذف مراجعة تعمل أو نتائج تعتمد عليها مراجعة غير مكتملة.

تتيح شاشة الملخصات حذف المحدد أو حذف القائمة المعروضة كاملة، للملخصات الأصلية ونتائج المراجعات. يعرض التأكيد عدد العناصر. تبقى العناصر غير المحددة وملفات PDF وسجلات الاستهلاك، ويُمنع الحذف أثناء إنتاج النتائج أو عند اعتماد مراجعة غير مكتملة عليها. حذف نتائج مهمة معلقة يلغي استئناف تلك المهمة حتى لا يعود عبر نتيجة محذوفة.
